This file has been superseded by docs/dev-loop.md.

Quick reference (full details there):

  Build  :  pwsh .\scripts\build.ps1
  Flash  :  pwsh .\scripts\flash.ps1            # auto-detect COM port
  Deploy :  pwsh .\scripts\sync_data.ps1 <board-ip>
  Mock   :  cd mock-server ; npm install ; npm run dev   # http://localhost:8765/

The previous text referenced a `build_and_monitor.sh` that does not exist in
this repo. The PowerShell scripts under scripts/ replace it.
